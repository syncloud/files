[uwsgi]
socket=${app_data_dir}/uwsgi/files.wsgi.sock
chdir = ${app_dir}/www
wsgi-file = ${app_data_dir}/uwsgi/files.wsgi
processes = 4
threads = 2
logto = ${app_data_dir}/log/uwsgi.log
module = files_uwsgi
callable = app
env = PYTHONHOME=${app_dir}/python
env = PYTHONPATH=${app_dir}/bin